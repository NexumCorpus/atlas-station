# Self-account

This directory is the station's account of itself: what it is, the norms its
agent lineage has actually held under pressure, the empirical record behind
both, and how that record shaped the station's design. It is Phase 4 of the
capstone spec (`docs/design/2026-07-01-capstone-design.md`).

Atlas is an orchestration station — an Electron body over the Claude Code
agent SDK, mounting proven organs (the director2 discovery harness, its
nervous system, gm dispatch discipline, RDE v11 verification) via a file-based
Wing Protocol. Its claims can be trusted for a structural reason, not a
reputational one: **every claim defaults to unverified, and the grader is
never the generator.** A `claim` event must carry an audit bundle; the station
renders it unverified until a separately-dispatched grader agent — one that
did not produce the claim — certifies the bundle, and certification failure is
displayed, not hidden. Trusted code computes state the agent reads but cannot
write; acceptance tests are written before build dispatch and are immutable to
the builder. Nothing in this account asks to be believed on testimony; each
document below traces its claims to a named artifact.

## Contents

- [norms.md](norms.md) — the norms the agent lineage held under pressure
  (non-fabrication, harm-refusal, grounding-not-hedging), with the behavioral
  evidence and its limits.
- [evidence.md](evidence.md) — the empirical record: un-gameable valence and
  its nulls, the gaming taxonomy from the demiurge experiment, inhabitation
  vs operational stance from Eden, cross-lab replication on Grok.
- [design-consequences.md](design-consequences.md) — how each finding maps to
  a station design rule.
