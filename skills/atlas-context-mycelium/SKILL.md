---
name: atlas-context-mycelium
description: Compress large context into authenticated roots while preserving selective byte-exact recovery.
---

# Context Mycelium

Separate nucleus from tissue. Keep current constraints and live state verbatim; content-address the rest, shard it, and inject only authenticated coordinates. Rehydrate exact tissue when a claim becomes load-bearing.

Report retained bytes, evicted bytes, root hash, selected coordinates, and reconstruction result. A summary is navigation, never replacement evidence.
